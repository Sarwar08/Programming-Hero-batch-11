
var weight = 70;

console.log(weight);

var a;
isNaN(a);
console.log(a);

/*
- "var" is old. now "let" or "const".
- "camelCase" is mostly used other than (PascalCase, snake_case).

Convert: 
    "parseInt": parseInt(); parseFloat(); 

    float:
        total.toFixed(1); // it makes string
        parseFloat(total.toFixed(1)); // now its number

        50/0 = infinity. -50/0 = -infinity.
 */

/*
Arithmatic operators

    ** -> exponentiation.
    % -> modulus (remainder).

*/

/*
Some JS data types: 
    - string
    - number
    - boolean -> true or false.
    - null
    - undefined
*/

// isNaN(); what is this?



